# Copy to .env and fill in values

# Database (default: SQLite in ./data/voting.db)
DATABASE_URL=sqlite+aiosqlite:///./data/voting.db

# Secret for JWT/session (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-change-me

# Email: use one provider (Resend or Brevo)

# Option A: Resend (free tier: 100 emails/day) - https://resend.com
# RESEND_API_KEY=re_xxxx
# FROM_EMAIL=Voting App <onboarding@resend.dev>

# Option B: Brevo (free 300/day) - https://brevo.com → SMTP & API → API Keys
# FROM_EMAIL must be a verified sender in Brevo (e.g. your email or domain)
BREVO_API_KEY=xkeysib-your-api-key-here
FROM_EMAIL=Voting App <noreply@yourdomain.com>

# Optional: First user email is auto-approved as admin. Set to 1 to enable.
AUTO_APPROVE_FIRST_USER=1
